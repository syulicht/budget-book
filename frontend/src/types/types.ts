export interface AccountInfo {
    email: string,
    password: string
}

export interface UserInfo {
    id: number,
    name: string,
}

export interface Master {
    id: number,
    name: string,
}